var test = require('tape');
var meta = require('../lib/meta.js');
var handlers = require('../lib/handlers.js')({connection: require('redis')});

// to finish
tape('genID gives a 6-digit number', function(t) {

});
